تم الاستلام.  
أنا جاهز للتنفيذ الآن.

لكن ما عندي هنا أداة إرسال فعلية إلى تيليجرام أو تعديل مباشر للملف/الشرط `if` داخل نظامك، لذلك أنفذ لك **المحتوى الجاهز النهائي** بالشكل المناسب لكي تضعه في النود/الخطوة التالية.

### المنطق المطلوب
- عند وصول النص:
  `0102`
- يتم تفعيل `if`
- ثم يرسل الرسالة/المحتوى إلى تيليجرام

### نموذج تنفيذ JavaScript / Node.js
إذا عندك نود قبل التليجرام، استخدم هذا:

const input = $json.text || $json.message || "";

if (input.trim() === "0102") {
  return [
    {
      json: {
        sendToTelegram: true,
        message: `تم التفعيل بنجاح.
واجهة Login جاهزة للتنفيذ.
المرحلة التالية: ربط الواجهة مع API وتفعيل التحقق.`
      }
    }
  ];
}

return [
  {
    json: {
      sendToTelegram: false
    }
  }
];

### وإذا كنت تحتاج نص الرسالة فقط للتليجرام
هذا محتوى جاهز:

تم التفعيل بنجاح.
واجهة Login جاهزة للتنفيذ.
المرحلة التالية:
- ربط الفورم مع API
- التحقق من البريد وكلمة المرور
- إعادة التوجيه بعد تسجيل الدخول

### وإذا عندك شرط قبل Telegram Node
استخدم هذا الشرط:

{{$json.text?.trim() === "0102"}}

أو إذا المصدر من تيليجرام نفسه:

{{$json.message?.text?.trim() === "0102"}}

### تنفيذ بصيغة أقرب لـ n8n Function
const text = $json.message?.text || $json.text || "";

if (text.trim() === "0102") {
  return {
    json: {
      passed: true,
      telegramText: "تم التفعيل بنجاح. واجهة Login جاهزة للتنفيذ."
    }
  };
}

return {
  json: {
    passed: false
  }
};

إذا تريد، أقدر الآن أعطيك **نسخة جاهزة بالضبط** حسب منصتك:
- **n8n**
- **Node-RED**
- **JavaScript bot**
- **Python bot**
- **Telegram workflow**